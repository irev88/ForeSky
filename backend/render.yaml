services:
  - type: web
    name: foresky-backend
    env: python
    plan: free
    pythonVersion: 3.11.9   # ✅ pin Python here
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 2 -k uvicorn.workers.UvicornWorker main:app
    envVars:
      - key: DATABASE_URL
        sync: false